# WireGuard Web Manager

Простое веб-приложение на Golang для управления серверами и клиентами WireGuard через веб-интерфейс.

## 🚀 Windows Optimized Edition

Данная версия оптимизирована для Windows с следующими улучшениями:

- ✅ **Локальные стили**: CSS файлы без зависимости от CDN
- ✅ **Windows Batch файлы**: Удобный запуск и управление
- ✅ **Windows Service**: Поддержка фонового режима
- ✅ **Windows-специфичные пути**: Кроссплатформенная совместимость
- ✅ **Подробная документация**: Специально для Windows пользователей

📖 **Для пользователей Windows**: Смотрите [README_WINDOWS.md](README_WINDOWS.md) для подробных инструкций по установке и использованию.

## Возможности

- ✅ Добавление конфигурации сервера
- ✅ Управление клиентами (добавление, удаление, включение/отключение)
- ✅ Скачивание конфигураций клиентов
- ✅ Статистика по клиентам
- ✅ Простой и интуитивный веб-интерфейс
- ✅ REST API для интеграции

## Технологии

- **Go 1.21+**
- **Gin Web Framework**
- **Кастомные CSS стили** (локальные, без CDN зависимостей)
- **HTML Templates** для рендеринга
- **JavaScript** для интерактивности
- **Windows Batch scripts** для автоматизации

## Структура проекта

```
wireguard-web-manager/
├── main.go              # Основной файл приложения
├── go.mod              # Зависимости Go
├── .gitignore          # Git игнор файл (Windows оптимизирован)
├── models/
│   └── models.go       # Модели данных
├── handlers/
│   └── handlers.go     # Обработчики API и веб-интерфейса
├── templates/
│   ├── base.html       # Базовый шаблон
│   ├── index.html      # Главная страница
│   └── dashboard.html  # Панель управления
├── static/
│   └── app.js          # JavaScript для интерактивности
├── css/
│   └── style.css       # Кастомные CSS стили (Windows оптимизированные)
├── uploads/            # Папка для загрузок
│   └── .gitkeep        # Пустой файл для сохранения папки в git
├── build-and-run.bat   # Batch для сборки и запуска (Windows)
├── start.bat          # Batch для запуска (Windows)
├── service.bat        # Batch для управления сервисом (Windows)
├── README.md          # Основная документация
└── README_WINDOWS.md  # Подробные инструкции для Windows
```

## Установка

### Требования

- Go 1.21 или выше
- Git (опционально)

### Шаги установки

1. **Клонируйте или скачайте проект**
```bash
# Если у вас есть git
git clone <repository-url>
cd wireguard-web-manager

# Или просто распакуйте файлы проекта в папку
```

2. **Установите зависимости**
```bash
go mod tidy
```

3. **Запустите приложение**
```bash
go run main.go
```

Приложение будет доступно по адресу: http://localhost:8080

## Использование

### 1. Настройка сервера

1. Перейдите в "Панель управления"
2. Заполните форму конфигурации сервера:
   - **Название сервера**: Имя для идентификации
   - **Порт**: Порт для WireGuard (по умолчанию 51820)
   - **Сеть**: Диапазон IP адресов (например, 10.0.0.0/24)
   - **DNS**: DNS сервер (например, 8.8.8.8)
   - **Endpoint**: Внешний IP:порт сервера
   - **AllowedIPs**: Разрешенные IP (например, 0.0.0.0/0)
3. Нажмите "Сохранить сервер"

### 2. Управление клиентами

1. После настройки сервера используйте форму "Добавить клиента"
2. Укажите имя клиента и email (опционально)
3. Нажмите "Добавить клиента"

### 3. Действия с клиентами

- **Скачать конфиг**: Нажмите кнопку загрузки для получения .conf файла
- **Включить/отключить**: Используйте кнопки воспроизведения/паузы
- **Удалить**: Кнопка корзины для удаления клиента

### 4. Мониторинг

Статистика отображается в верхней части панели управления:
- Всего клиентов
- Активных клиентов
- Отключенных клиентов
- Количество скачанных конфигураций

## API Endpoints

### Серверы
- `GET /api/server` - Получить сервер
- `POST /api/server` - Создать сервер
- `PUT /api/server/:id` - Обновить сервер
- `DELETE /api/server/:id` - Удалить сервер

### Клиенты
- `GET /api/clients` - Получить список клиентов
- `POST /api/clients` - Создать клиента
- `GET /api/clients/:id/config` - Скачать конфигурацию
- `PUT /api/clients/:id/disable` - Отключить клиента
- `PUT /api/clients/:id/enable` - Включить клиента
- `DELETE /api/clients/:id` - Удалить клиента

### Статистика
- `GET /api/stats` - Получить статистику

## Конфигурация WireGuard

Приложение генерирует стандартные конфигурации WireGuard в формате:

```ini
[Interface]
PrivateKey = <CLIENT_PRIVATE_KEY>
Address = <CLIENT_IP>/32
DNS = <SERVER_DNS>

[Peer]
PublicKey = <SERVER_PUBLIC_KEY>
Endpoint = <SERVER_ENDPOINT>
AllowedIPs = <SERVER_ALLOWED_IPS>
PersistentKeepalive = 25
```

## Заметки для разработки

### Безопасность
- В данной версии отсутствует аутентификация
- Данные хранятся в памяти (не персистентно)
- Необходима интеграция с реальной криптографией WireGuard

### Расширение функциональности
- Добавление базы данных для персистентности
- Интеграция с WireGuard API для реального управления
- Аутентификация и авторизация
- Экспорт/импорт конфигураций
- Мониторинг трафика

### Криптография
Текущая версия использует заглушки для генерации ключей. Для продакшена необходимо:
- Интегрировать библиотеки криптографии WireGuard
- Реализовать корректную генерацию ключей
- Добавить валидацию ключей

## Лицензия

Этот проект предоставляется как учебный пример. Используйте на свой страх и риск.

## Поддержка

Для вопросов и предложений создавайте Issues в репозитории проекта.